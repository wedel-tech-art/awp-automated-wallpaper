conky.config = {
    alignment = 'top_right',
    background = true,
    double_buffer = true,
    own_window = true,
    own_window_type = 'desktop',
    own_window_transparent = true,
    own_window_argb_visual = true,
    own_window_argb_value = 0,
    
    -- Load the Lua script
    lua_load = 'draw_all.lua',
    lua_draw_hook_pre = 'conky_draw_bg',
    lua_draw_hook_post = 'conky_render_logo_top',

    gap_x = 30,
    gap_y = 60,
    minimum_width = 200,
    minimum_height = 100,

    use_xft = true,
    font = 'DejaVu Sans Mono:size=10',
    default_color = 'white',
}

conky.text = [[
${color grey}SYSTEM INFO${hr}
CPU: ${alignr}${cpu}%
RAM: ${alignr}${memperc}%
UPT: ${alignr}${uptime_short}


${color grey}AWP STATUS${hr}
WORKSPACE: ${alignr}${exec cat .awp_conky_state.txt | grep workspace_name | cut -d= -f2}
]]
